# AGENTS.md

## このリポジトリの目的

複雑化した Stormworks 向け鉄道車両マイコンを、人間と AI の双方が読める形へ
起こし直し、保守可能にするためのリポジトリである。

したがって、単にゲーム内で動くことや元 XML と同じ出力になることだけを完成とは
みなさない。信号の意味、状態遷移、安全条件、生成元と生成物の関係を説明・検証
できる状態を保つ。

## 何を正典とするか

現行の `SPEC.md` / `SIGNAL_MAP.md` と実装（`.sw-net` / `src/*.lua`）を中心に読む。
コードだけでは分からない判断理由は `DESIGN_LOG.md` にある。

これは、外部資料や旧実装が現在の設計と意図的に異なることがあるためである。
`EXTERNAL_SOURCE_NOTES.md`、公開資料、Dropbox 上の旧資産は背景の理解には使えるが、
それらとの差を自動的に不具合と判断しない。文書と実装が食い違う場合も、片方へ
機械的に合わせず、テストと設計理由から現在の意図を確かめる。

## 同じロジックが複数の姿を持つ理由

Stormworks の XML は実機との受け渡しに必要だが、そのままでは読解・保守しにくい。
そのため `storm-mcl` の `.sw-mcl` / `.sw-net`、編集用の `src/*.lua`、実機投入用の
`deploy/*_deploy.lua` など、目的の異なる表現を併存させている。

編集前に、どれが人間が直す正本で、どれが取り込み結果または生成物かを確認する。
生成物を直接直すと次の生成で変更が失われ、複数の書き手を作るとどちらが正しいか
分からなくなるためである。特に Driver Assistance の `scripts/n61.lua` と
`scripts/n141.lua` は XML 再取り込み側が書く場所であり、deploy ビルドの出力を
暗黙に反映しない。

## tick と信号の意味を守る理由

このシステムでは、値だけでなく「何 tick 後にその値になるか」が挙動の一部である。
tick評価の一般仕様は`storm-mcl spec`を正典とする。遅延を無視して単純化すると、
非常制動、リセット、再力行などの過渡状態を壊し得る。

composite のチャンネル番号、負論理、failsafe、ラッチの set/reset は名前から
推測しない。同じ N1/N2 でもインターフェースによりブレーキと力行の割当が異なる
箇所があり、読み書き双方の接続が契約だからである。割付を変える場合は、実装だけで
なく encode/decode、信号表、仕様、テストを同時に整合させる。

## Lua が一般的でない構造を取る理由

Luaノードの制約値は`storm-mcl spec LUA`を正典とする。この制約のため、実機投入
経路では module table を避け、外部 API をグローバル関数、内部実装を `local` とし、
基本的に `dofile` で平坦化する。名前付き table より位置引数や複数戻り値を使うのも、
minify 後の文字数を抑えるためである。詳しい契約は変更前に
`LUA_CODING_GUIDE.md` を読む。

この妥協は実機投入コードのためのもので、テストや補助ツールまで不自然に圧縮する
理由にはならない。また、グローバル名の衝突や演算子優先順位は minify 後にだけ
壊れることがあるため、ソースと deploy 成果物の双方を検証する。生成後の合否は
テキスト差分の小ささではなく、Luaノードの制約内であることと回帰テストで判断する。

ビルド支援を Node.js で書くのは、主な開発環境である Windows でも同じ工程を
実行可能にするためである。

## 読める `.sw-net` を保つ理由

`n123` のような自動生成名だけでは、仕様と接続の意図を追跡できない。ノードには
役割を表す英語の `snake_case` 名を付け、信号の流れに沿って配置する。

一方、並べ替えはロジック変更ではない。並べ替え前の内容を残して
`verify_sort.sh` で欠落を検査するのは、可読性改善に機能変更を紛れ込ませないため
である。構文やノード固有の挙動が不明なら、一般的な回路知識で補わず
`storm-mcl spec` を参照する。

## 完了の考え方

変更した層に対応する検証を行う。`.sw-net` なら typecheck と必要なシミュレーション、
Lua ならソースと生成物の回帰テスト、生成物ならサイズと生成元との対応、信号なら
仕様と encode/decode の整合を確認する。

検証の目的はチェック項目を消化することではなく、意図した定常挙動と過渡挙動を
保ち、正本から成果物を再生成できると示すことである。未検証事項は明記する。
コミットや push は成果物の作成とは別の操作なので、明示的に依頼された場合だけ行う。
